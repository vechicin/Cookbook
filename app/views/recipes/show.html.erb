<div>
  <%= link_to "Back to recipes", url_for(:back) %>
  <% if can? :destroy, @recipe %>
    <%= button_to "Remove", @recipe, method: :delete %>
  <% end %>
</div>

<%= render @recipe %>

<div>
  <% if can? :destroy, @recipe %>
    <a href="/shopping_list/<%= @recipe.id %>">Generate Shopping List</a>
    <a href="/recipe_foods/<%= @recipe.id %>">Add Ingredient</a>
  <% end %>
</div>

<%= render 'recipe_ingredients' %>