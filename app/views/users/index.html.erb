<%= button_to 'Sign Out', destroy_user_session_path, method: :delete %>

<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Go to user page", user %>
    </p>
  <% end %>
</div>
